import React from 'react';
import { PIconButtonProps as Props } from './PIconButton.types';
export declare const PIconButton: ({ children, className, sx: initSx, size, color: initColor, iconSize, iconProps, tooltip, tooltipPlacement, tooltipProps, fullWidth, ...props }: Props) => React.JSX.Element;
export default PIconButton;
