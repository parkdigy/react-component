import React from 'react';
import { type PStackProps as Props } from './PStack.types';
export declare const PStack: ({ className, row, span, inline, center, centerJustifyContent, gap, spacing, flexWrap, wrap, alignItems, justifyContent, ...props }: Props) => React.JSX.Element;
export default PStack;
