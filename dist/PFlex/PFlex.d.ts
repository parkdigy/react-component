import React from 'react';
import { PFlexProps as Props } from './PFlex.types';
export declare const PFlex: ({ className, row, spacing, alignCenter, justifyCenter, alignItems, justifyContent, gap, ...props }: Props) => React.JSX.Element;
export default PFlex;
