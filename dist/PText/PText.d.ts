import React from 'react';
import { PTextProps as Props } from './PText.types';
export declare const PText: ({ display, line, center, className, size, color, helper, ph, pv, paddingLeft, paddingRight, paddingTop, paddingBottom, mh, mv, marginLeft, marginRight, marginTop, marginBottom, fullSize, fullWidth, fullHeight, width, height, children, ...initProps }: Props) => React.JSX.Element;
export default PText;
