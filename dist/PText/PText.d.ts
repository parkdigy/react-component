import React from 'react';
import { type PTextProps as Props } from './PText.types';
export declare const PText: ({ display, line, center, className, size, color, helper, ph, pv, mh, mv, fullWidth, fullHeight, fullSize, children, ...initProps }: Props) => React.JSX.Element;
export default PText;
