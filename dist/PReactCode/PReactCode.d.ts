import React from 'react';
import { type PReactCodeProps as Props } from './PReactCode.types';
export declare const PReactCode: ({ className, name, content, props, ...boxProps }: Props) => React.JSX.Element;
export default PReactCode;
