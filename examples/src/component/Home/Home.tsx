import React from 'react';
import { PButton, PNumberText } from '../../../../src';

const Home = () => {
  return (
    <PButton>
      <PNumberText value='123' />
    </PButton>
  );
};

export default Home;
